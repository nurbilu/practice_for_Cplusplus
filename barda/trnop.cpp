#include <iostream>

int main(){
    // ternary operator ?: repalces if-else statements 
    // condition ? expression1 : expression2 ;

    // int grade = 75;

    // grade >= 60 ? std::cout << "Passed" : std::cout << "Failed";

    // int number = 8;
    
    // number % 2 == 1 ? std::cout << "Odd" : std::cout << "Even";

    // number % 2 ? std::cout << "Even" : std::cout << "Odd";

    // bool hungry = true;

    // hungry ? std::cout << "Eat something" : std::cout << "You are full";

    // std::cout << (hungry ? "Eat something" : "You are full");
    


    return 0;
}