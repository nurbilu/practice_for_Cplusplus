#include <iostream> 

int main(){
    // int temp;

    // std:cout << "Enter the temperature: ";
    // std::cin >> temp;

    // if (temp > 0 && temp < 30)
    //     std::cout << "The weather is good.";
    // else if (temp <=0  || temp >= 30)
    //     std::cout << "The weather is bad.";
    // else
    //     std::cout << "The weather is cold.";

    bool sunny = true;

    if (!sunny)
        std::cout << "The weather is bad.";
    else
        std::cout << "The weather is good.";
    
}